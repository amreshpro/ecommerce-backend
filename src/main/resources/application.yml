spring:
  application:
    name: ecommerce
  datasource:
    url: jdbc:mysql://localhost:5432/ecommerce
    username: postgres
    password: amreshpro
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true